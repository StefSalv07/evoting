function handleTokenExpiration(email) {
  // Handle token expiration logic here, such as refreshing tokens or logging out the user
  console.log(`Token expired for user ${email}`);
}

module.exports = { handleTokenExpiration };
